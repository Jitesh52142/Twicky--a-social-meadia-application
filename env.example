# Django Settings
# Generate a new SECRET_KEY using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-replace-this-with-generated-key

# Set to False in production, True for local development
DEBUG=False

# Add your Vercel domain after deployment
ALLOWED_HOSTS=localhost,127.0.0.1,.vercel.app,your-app-name.vercel.app

# MongoDB Connection (Optional - currently using SQLite for Vercel)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/twicky

# For production, use a different database user with restricted permissions

